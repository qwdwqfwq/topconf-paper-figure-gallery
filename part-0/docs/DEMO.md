# Demo asset

`demo.gif` is a small, generated preview of the gallery interaction. It uses
real gallery crops and labels the two key interactions: text search and the
lightbox. The website itself remains a zero-build static page; regenerate the
asset with `python scripts/make_demo_gif.py` after changing the design.
